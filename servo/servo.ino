#include <Servo.h>
Servo myservo;
int knopkapin = 10;

void setup()
{
  pinMode(myservo, attach(8);
  pinMode(knipkapin);
}
void loop()
{
buttonState = digitalRead(buttonPin);
if(buttonState == HIGH)
{
  digitalWrite(myservo, 180);
}
}

 
